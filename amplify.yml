version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No dependencies to install"
    build:
      commands:
        - echo "No build process needed for static site"
  artifacts:
    baseDirectory: /frontend
    files:
      - '**/*'
  cache:
    paths: []
